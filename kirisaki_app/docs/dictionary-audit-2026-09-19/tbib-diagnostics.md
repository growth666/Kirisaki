# TBIB 连接专项排查

2026-09-19，Windows，本机直连，无代理。未修改应用配置或 CSV 词库。

系统 DNS 返回 A：104.21.88.140、172.67.180.124；AAAA：2606:4700:3035::ac43:b47c、2606:4700:3037::6815:588c。

通过 curl `--resolve` 分别连接两个当前 A 记录，保持域名、SNI 与证书校验，两者均返回连接重置，未收到 HTTP 响应。强制 TLS 1.2 同样失败。curl 强制 IPv6 返回无法解析主机，未验证 IPv6 连通性；系统存在 AAAA 记录不等于本机具有可用 IPv6 网络。

此前 Dart 应用客户端也报告 TLS 握手中断，www 官方子域名的 curl 测试同样重置。因此不能将问题归因于单个地址、Dart JSON 解析器或中文关键词。无法仅凭本机结果区分本地网络路径、运营商、中间设备与源站策略。

原始结果见 `tbib-diagnostics.json`。未降级 HTTP、关闭证书检查、硬编码 IP 或引入第三方中转；这些不是已验证的修复。应用现有代理配置保持不变。

当前结论：未恢复 TBIB。继续定位需要另一网络（如手机热点）对照，或用户已有的可用 HTTP 代理；本次没有可用代理。其他正常图源仍可使用，可在图源管理中临时禁用 TBIB。不得将旧的第三方网站在线状态记录当作本次接口可用的证明。
